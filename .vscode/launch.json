{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "chrome",
            "request": "launch",
            "name": "Launch Chrome against localhost",
            "url": "http://localhost:5500", // Uprav podle portu, na kterém běží tvá PWA
            "webRoot": "${workspaceFolder}"
        }
    ]
}