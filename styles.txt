body {
    margin: 0;
    height: 100vh;
    display: block;
    /* Změníme na block, abychom odstranili flex omezení */
    background-color: #f0f0f0;
    font-family: Arial, sans-serif;
    overflow-x: hidden;
    /* Zabrání horizontálnímu scrollování */
    box-sizing: border-box;
    /* Zajistíme správné výpočty šířky/výšky */
}

html {
    width: 100%;
    height: 100%;
    overflow-x: hidden;
    /* Zabrání horizontálnímu scrollování */
}

.container {
    width: 100%;
    height: 100%;
    display: flex;
    flex-direction: column;
    position: relative;
    /* Pro umístění videa v rámci kontejneru */
    max-width: none !important;
    /* Odstraníme případné omezení šířky */
    box-sizing: border-box;
    /* Zajistíme správné výpočty */
}

.top-half,
.bottom-half {
    flex: 1;
    display: flex;
    justify-content: center;
    align-items: center;
    max-width: none !important;
    /* Odstraníme případné omezení */
    box-sizing: border-box;
    /* Zajistíme správné výpočty */
}

.mic-icon {
    width: 250px;
    /* Zachováme zmenšení mikrofonu na desktopu (můžeme upravit) */
    height: auto;
    /* Zachování poměru */
    cursor: pointer;
    transition: opacity 0.3s;
    /* Změníme na opacity pro lepší efekt */
    background: transparent;
    /* Bílé pozadí pro lepší kontrast na desktopu */
    border-radius: 10px;
    /* Lehké zaokrouhlení pro estetiku */
    padding: 5px;
    /* Levé okraje pro průhlednou ikonu */
    box-sizing: border-box;
    /* Zajistíme správné výpočty */
}

.mic-icon.recording {
    opacity: 0.7;
    /* Změníme průhlednost během nahrávání */
}

/* Styly pro stavové zprávy */
.status {
    font-size: 24px;
    color: #4444ff;
    /* Modrá barva pro stav */
    font-weight: normal;
    /* Neboldované */
    display: block;
    margin-bottom: 10px;
}

.hint {
    font-size: 18px;
    color: #333;
    /* Černá barva pro nápovědu */
    display: block;
}

/* Desktop – zvětšené video pod mikrofonem, správný poměr stran, minimální mezera */
@media (min-width: 769px) {
    .mic-icon {
        width: 250px;
        /* Zachováme zmenšení mikrofonu */
    }

    .container,
    .bottom-half {
        max-width: none !important;
        /* Odstraníme případné omezení */
        width: 100%;
        /* Plná šířka kontejneru */
    }

    #output {
        position: relative;
        /* Vrátíme video do .bottom-half */
        width: 533px !important;
        /* Zachováme zvětšení videa */
        margin: 0 auto !important;
        /* Centrování videa v rámci stránky */
        padding: 0 !important;
        /* Odstraníme padding */
    }
}

/* Mobil – video pod mikrofonem, bez modrého okraje u mikrofonu */
@media (max-width: 768px) {
    .mic-icon {
        width: 50vw;
        /* Polovina šířky mobilu */
        max-width: none;
        /* Žádné omezení na mobilu */
        background: transparent;
        /* Bez pozadí na mobilu, aby kontrastoval s modrou plochou */
        border: none;
        /* Odstraníme modrý okraj */
        box-sizing: border-box;
        /* Zajistíme správné výpočty */
    }

    .bottom-half {
        align-items: flex-start;
        /* Posuneme obsah (.bottom-half) nahoru na mobilu */
        padding-top: 80px;
        /* Zůstáváme u 80px pro posun nahoru */
    }

    #output audio,
    #output video,
    #output iframe {
        height: 400px;
        /* Zachováme původní výšku pro mobil */
        max-width: 100%;
        /* Normální šířka na mobilu */
        object-fit: contain;
        /* Zachování proporce */
        box-sizing: border-box;
        /* Zajistíme správné výpočty */
    }
}

/* ... vaše stávající styly ... */
.pulsate {
    animation: pulsate-animation 1s infinite alternate;
}

@keyframes pulsate-animation {
    0% {
        opacity: 0.7;
        transform: scale(1);
    }

    100% {
        opacity: 1;
        transform: scale(1.1);
    }
}

/* styles.css */
/* ... vaše stávající CSS styly ... */

#output {
    width: 90% !important;
    /* Zvětšení šířky boxu a vynucení přepisu */
    background-color: rgba(200, 200, 200, 0.2);
    color: #333;
    padding: 10px;
    margin-top: 10px;
    border-left: 5px solid #888;
    text-align: left;
    font-size: 1.4em;
    box-sizing: border-box;
    margin-left: 20px !important;
    /* Větší odsazení od levého okraje a vynucení přepisu */
}

#output.default-text {
    width: 90% !important;
    /* Zvětšení šířky boxu i pro výchozí text */
    background-color: rgba(128, 128, 128, 0.2);
    border-left-color: 5px solid #888 !important;
    padding: 10px;
    /* Stejný padding pro konzistenci */
    margin-top: 10px;
    /* Stejná mezera od mikrofonu */
    text-align: center;
    font-size: 1.4em;
    box-sizing: border-box;
    margin-left: 20px !important;
    /* Větší odsazení od levého okraje i pro výchozí text */
}

/* Styly pro různé závažnosti notifikací (ponecháme s !important pro jistotu) */
.notification-urgent {
    border-left-color: red !important;
    background-color: rgba(255, 0, 0, 0.1) !important;
    font-size: 1.4em !important;
}

.notification-informative {
    border-left-color: blue !important;
    background-color: rgba(0, 0, 255, 0.1) !important;
    font-size: 1.4em !important;
}

.notification-ok {
    border-left-color: green !important;
    background-color: rgba(0, 255, 0, 0.1) !important;
    font-size: 1.4em !important;
}

.notification-warning {
    border-left-color: orange !important;
    background-color: rgba(255, 165, 0, 0.1) !important;
    font-size: 1.4em !important;
}